#!/usr/bin/env python3
"""Fail-closed tombstone for the retired sub2cli CLI."""

import sys


def main() -> int:
    print(
        "sub2cli is retired (EOL 2026-07-22); this command is disabled and made no changes.",
        file=sys.stderr,
    )
    return 64


if __name__ == "__main__":
    raise SystemExit(main())
