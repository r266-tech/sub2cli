# AGENTS.md — retired repository boundary

sub2cli reached end of life on 2026-07-22.

Do not treat this repository as an active project, configuration source,
control plane, or dependency. Do not run its app, CLI, injector, proxy,
installers, tests, release scripts, or credential/profile tooling unless the
current user request explicitly names and authorizes a sub2cli retirement,
rollback, security, archive, or historical task.

sub2cli is not part of Sub2API. Never infer Sub2API server, upstream, release,
automation, credential, or Codex-profile scope from files in this repository.
For a Sub2API request, leave this repository and use only the independently
named Sub2API project or its dedicated operations controller.

Preserve historical files and quarantined state. Rollback must name the exact
surface to restore and must prove that Sub2API remains independent. Do not
restore the whole project for convenience.
